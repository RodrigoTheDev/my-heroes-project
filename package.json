{
  "name": "my-heroes",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch db/heroes.json --port 3000",
    "userdb": "json-server --watch db/users.json --port 3001"

  },
  "dependencies": {
    "json-server": "^0.17.3",
    "vue": "^3.3.4",
    "vue-router": "^4.2.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.3.1",
    "vite": "^4.4.9"
  }
}
